# Basic_Java_AI
Basic AI in Java

SimpBot.java 
  Creates a simple chat for which valid inputs are:
  "hello", "hi", "how are you" & "bye" (which calls dispose()).
  Any other input values will result in one of 3 randomly generated responses.
